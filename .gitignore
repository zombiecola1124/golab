# 환경변수 (API 키 포함 — 절대 커밋 금지)
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node.js
node_modules/

# Firebase 서비스 계정 (절대 커밋 금지)
service-account.json
**/service-account*.json

# Claude Code launch.json (로컬 절대 경로 포함 — PC별 독립 관리)
.claude/launch.json

# Firebase Hosting 캐시 (로컬 빌드 산출물)
.firebase/

# 테스트 스크린샷 (로컬 생성물)
test_*.png

# 리포트 (로컬 생성물)
research/genspark_report.json
