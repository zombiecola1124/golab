# golab - 1인사업자 관리프로그램
# 가상환경 설정 후 설치: pip install -r requirements.txt

# 테스트
pytest>=8.0
pytest-cov>=4.0

# 데이터베이스 (SQLite는 표준 라이브러리에 포함)

# 멀티 AI 엔진 (GPT / Gemini)
openai>=1.0
google-generativeai>=0.5

# 유틸리티
python-dateutil>=2.9
python-dotenv>=1.0
